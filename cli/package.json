{
  "name": "envvault",
  "version": "0.1.0",
  "description": "EnvVault CLI — securely sync encrypted environment variables from the cloud.",
  "type": "module",
  "bin": {
    "envvault": "./bin/envvault.js"
  },
  "files": [
    "bin",
    "dist"
  ],
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "build": "tsc",
    "dev": "tsc --watch",
    "start": "node bin/envvault.js"
  },
  "dependencies": {
    "chalk": "^5.3.0",
    "commander": "^12.1.0",
    "conf": "^13.0.1",
    "ora": "^8.1.0"
  },
  "devDependencies": {
    "@types/node": "^20.14.0",
    "typescript": "^5.4.0"
  }
}
