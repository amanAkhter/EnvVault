{
  "name": "envvault-functions",
  "description": "EnvVault Cloud Functions (design reference — not yet deployed)",
  "version": "0.1.0",
  "private": true,
  "engines": {
    "node": "20"
  },
  "main": "lib/index.js",
  "scripts": {
    "build": "tsc",
    "serve": "npm run build && firebase emulators:start --only functions",
    "deploy": "firebase deploy --only functions"
  },
  "dependencies": {
    "firebase-admin": "^12.0.0",
    "firebase-functions": "^5.0.0",
    "resend": "^4.8.0"
  },
  "devDependencies": {
    "typescript": "^5.4.0"
  }
}
